# HANDOFF — KingStackApp

## 現在地

v1.5。仕様書のPhase 1〜3を実装済み。Phase 4はDaily Challengeのみ着手。

### v1.5で変えたこと（UI・操作）
- 重ね順を正順に戻した（右のカードほど手前）。最新カードが右端で全面見え、隠れるのは左側になる。
  これに合わせてインデックスは左上と右上の2箇所・どちらも正立。`locate()` も「その座標で最も番号の大きいカード」を返す向きに変更。
- 操作ボタンをすべて盤面の上へ集約（MENU / Undo / Hint / リドロー）。MENUはUndoの左。
- スコア行を1行に圧縮し、盤面は `weight(1f)` で残り高さを総取り。カードの寸法と文字サイズは
  `rowHeight` から算出するので、盤面が広がるぶんだけ自動で大きくなる。
- 残り枚数は配るボタンの直上にのみ表示。
- ドラッグ追従の最適化: 指の座標は `dragPoint` として保持し、読むのは `Modifier.offset` のラムダの中だけ。
  合成をやり直さずレイアウトだけが走る。加えて、同じカードを指している間は `onDragMove` を呼ばない
  （`lastHit` は pointerInput のブロック内のローカル変数なので、比較自体が再合成を起こさない）。

### v1.4で変えたこと（UI・操作）
- カードの数字とマークを右上と左下の2箇所に配置（左下は180度回転）。トランプと同じ両端インデックス。
- 実際のドラッグ&ドロップ表示: 指が元の列から出た時点で、選択中のランを列から抜いて指の位置に浮かせて描く
  （`BoardView` 内の `dragPos` / `carrying`）。運搬中の重ね順は自然順なので、隠れる側は左下の逆さ表示で読める。
- 6列目・7列目のロック帯を画面上部に移動。表示順は `DISPLAY_ORDER = [6, 5, 0, 1, 2, 3, 4]` の1箇所で決まる。
  `locate()` も同じ配列を通すので、並べ替えても当たり判定はずれない。

### v1.2で変えたこと（UI・操作）
- 盤面を縦7列から**横7行**に変更（`ui/BoardView.kt` 全面書き換え）。1枚目が左端、右へ行くほど新しい。
- 重ね順を反転させ、若い番号のカードほど手前に描く。これで全カードの**右端**が必ず露出するので、
  数字とマークを右端に寄せれば常に見える。選択中のランだけは最前面に描いて全体を見せる。
- なぞり選択: `GameController.dragStart / dragMove / dragEnd`。同じ行の上では最後に触れたカードを
  アクティブにするが、`Rules.isMovableRun` が false のカードは飛ばして直前の選択を維持する。
- 帯ドロップ: 別の行の帯（端から端・全幅）に指が入ったらその行が移動先候補。カードに重ねる必要はなく、
  帯のどこで離してもその行の末尾に置く。
- リドロー機能を設定でON/OFF（`Settings.redrawEnabled`）。OFFでボタンごと消える。

ヒットテストは `BoardView.locate()` に集約。各カードの当たり幅は step とちょうど一致し、
見えている右端の帯と操作範囲がずれない。

### v1.1で直したこと
- キング開放が実質発火しなかった問題: 「先頭＝最下段」限定だったため、プレイヤーの操作でほぼ到達できなかった。
  最下段に加えて「列の一番上に露出したキング」でも開放するようにした（`Engine.refresh`）。
- 厳格モードで手詰まりでも配札できなかった問題: 「合法手ゼロ」を条件にしていたが、意味のない移動が常に残るため
  ほぼ成立しなかった。`Rules.productiveMoves`（置いて隣接が増える手）がゼロなら手詰まり、という定義に変更。
  GAME OVER判定も同じ定義に合わせた。
- `app/debug.keystore` を固定し、以後は上書きインストール可能にした。

## 未実装 / 次にやること

- ドラッグ＆ドロップ移動（現状はタップ選択→タップ移動のみ）
- 配札・完成時のカードアニメーション（現状は選択時のリフトのみ）
- ランキング、実績、ミッション、カスタムカード
- ジョーカー・特殊カード・8列以上ステージ
- 実プレイデータによる `BoardAnalyzer` の重み調整（現状は仕様書の仮重みのまま）

## 触る場所の目安

| やりたいこと | ファイル |
|---|---|
| 列数・完成枚数・キング開放回数 | `core/Model.kt` の定数 |
| 置ける条件・一括移動条件・完成条件 | `core/Rules.kt` |
| スコア配分・配札処理・勝敗判定 | `core/Engine.kt` |
| AIの強さ・評価重み | `ai/BoardAnalyzer.kt`, `ai/DrawAI.kt` |
| 難易度カーブ | `core/Model.kt` の `Difficulty`, `ai/DrawAI.kt` の `DifficultyAI` |
| 盤面の見た目・カード寸法 | `ui/BoardView.kt` |

## 注意

- `Engine.refresh()` は盤面変化後に必ず通す。完成退避・キング開放・勝敗判定がここに集約されている。
- `GameState` は不変。Undoはスナップショットのスタック（最大60）。
- セーブはカードIDのみをSharedPreferencesにJSON保存。`Card(id)` から suit/rank を復元する設計。
